Integrantes:

Claros Diaz Jair
Morro Luciano
Cespedez Pozo Leandro

a- El master queda con los archivos unificados de la rama Morro y la rama Claros
b- Si, pude ver todos los cambios 
c- Para hacer el merge primero hice el git clone del repositorio, después me metí a la carpeta del clone, y ejecute el git bash, puse el comando git fetch --all, git branch ( para asegurarme que estoy en el main ), hice un git merge --no-ff nombre_de_la_rama -m "comentario" y por ultimo hice un git push origin main ( para subir el merge al GitHub )

d-Después de que BETA realiza el merge con la rama main, la rama de BETA y la rama de GAMMA se actualizaron automáticamente al usar git checkout <rama>, git pull origin master

e-Los errores que hubo es que, dependiendo de como hacias el merge podias terminar combinando todas las ramas en una. La forma correcta que usamos fue:
git checkout main
Hacer un merge forzado (no fast-forward)
git merge --no-ff morro -m "Merge de la rama morro a master"

Subís los cambios
git push origin main
hice eso

f-git branch -a
git checkout apellido de beta
git pull origin apellido de beta
Notepad beta.txt
git add beta.txt
git commit -m "Agregue nombre, hora y la informacion de ifconfig a beta.txt"
git push origin apellido de beta

g-Los comandos que uso ALFA para poder hacer lo pedido en el punto 8 son:
git fetch origin
git branch -a 
git checkout rama de BETA
git add .
git commit -m "modifique el archivo beta.txt"
git push 